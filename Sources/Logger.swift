import Foundation
import os.log

public let networkLogger = Logger(subsystem: "com.posusume.log", category: "Network")
public let authLogger = Logger(subsystem: "com.posusume.log", category: "Auth")

